
<div class="col-lg-3 col-md-4 col-6">
    <div class="product">
        {# <div class="product_img">
            <a href="">
                        {% for image in product.images %}
            <div class="product-image">
                <img src="{{ image.imagePath }}" alt="product_image">
            </div>
        {% else %}
            <div class="product-image">
                <img src= assets/uploads/products/{{product.image}} alt="product image">
            </div>
        {% endfor %}
            </a> #}
  {# {% for image in product.images %}
    <img src="{{ asset('uploads/products/' ~ image.imageName) }}" alt="{{ product.name }}" />
{% endfor %} #}
{% for productImage in product.productImages %}
    <img src="{{ asset(field.getCustomOption('base_path') ~ '/' ~ image.fileName) }}" width="50" height="50" />
{% endfor %}
            <div class="product_action_box">
                <ul class="list_none pr_action_btn">
                    <li class="add-to-cart"><a href="#"><i class="icon-basket-loaded"></i> Add To Cart</a></li>
                    <li><a href="" class=""><i class="icon-shuffle"></i></a></li>
                    <li><a href="" class=""><i class="icon-magnifier-add"></i></a></li>
                    <li><a href="#"><i class="icon-heart"></i></a></li>
               </ul>
            </div>
        </div>
        <div class="product_info">
            <h6 class="product_title"><a href="">{{product.name}}</a></h6>
            <div class="product_price">
                <span class="price">{{product.price/100}}</span>
                {# <del>{{product.price1*35/100}}</del> #}
                <div class="on_sale">
                    <span>35% Off</span>
                </div>
            </div>
            <div class="rating_wrap">
                <div class="rating">
                    <div class="product_rate" style="width:80%"></div>
                </div>
                {# <span class="rating_num">{{product.reviewsProduct | length}}</span> #}
            </div>
            <div class="pr_desc">
                <p>{{product.description}}</p>
            </div>
       </div>
    </div>
</div>
