 {{ form_start(form) }}
        {{ form_row(form.fullName) }}
        {{ form_row(form.campany) }}
        {{ form_row(form.address) }}
        {{ form_row(form.complement) }}
        {{ form_row(form.phone) }}
        {{ form_row(form.city) }}
        {{ form_row(form.codePostal) }}
        <div class="form-group">
            <div>
                {{ form_label(form.country, 'Pays') }}
            </div>
            {{ form_widget(form.country, {'attr': {'style': 'font-size: 1rem; padding: 10px; width: 100%; box-sizing: border-box;'}}) }}
        </div>
    <button class="btn btn-border-fill btn-block mt-2">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}